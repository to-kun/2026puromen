# 小出市役所 公式ホームページ（異変探しゲーム）

8番出口ライクな、変化（異変）を見つけて脱出を目指すWebゲームです。  
小出市役所の公式ホームページを模したサイト内を観察し、異変の有無を正しく判定して階層 8 からの脱出を目指します。

---

## 🎮 ゲームルール

1. **基本ルール**
   - プレイヤーは小出市役所のホームページ内を探索します。
   - ページ内に「異変（通常と異なる点）」があるか注意深く確認します。
   - ナビゲーションの **「報告・お問い合わせ」** タブから異変の有無を選択・報告します。
     - **異変がある** と思った場合：`異変がある` ボタンを押す
     - **異変がない** と思った場合：`異変はない` ボタンを押す
   - **正解** すると次の階層へ進みます（階層 8 に到達すると脱出成功）。
   - **不正解** になると振り出し（階層 0）に戻されます。

2. **初回プレイ（階層 0）**
   - 初回開始時（`階層 0`）は、正解の基準となる状態を確認できるよう **必ず「異変なし」** に設定されています。

3. **振り返り機能**
   - 初回プレイ以外のゲーム進行中や失敗時、画面A（案内画面）に **「直前のページを振り返る」** ボタンが表示されます。
   - このボタンを押すと、判定に影響を与えない閲覧専用モードで直前のページ状態を再確認できます。

---

## 📁 ディレクトリ構成

```text
.
├── app.py                   # Flaskアプリケーション本体（ローカル開発・ビルド用）
├── freeze.py                # Frozen-Flask用静的サイト出力スクリプト
├── templates/
│   └── index.html           # メインHTML（SPA構造）
└── static/
    ├── css/
    │   └── style.css        # 全体スタイル・レイアウト定義
    ├── js/
    │   ├── anomalies.js     # 異変データ・AnomalyManagerクラス定義
    │   ├── gameLogic.js     # ゲーム進行・状態管理（GameLogicクラス）
    │   └── main.js          # イベントリスナー登録・DOM操作
    └── images/              # 画像素材（ヘッダー、バナー、職員写真等）

```
---

## 🛠 動作環境・開発手順

### ローカルでの動作確認（Flask）

1. 必要なライブラリのインストール
   ```bash
   pip install Flask Frozen-Flask
   ```
2. Flaskサーバーの起動

```Bash
python app.py
```

ブラウザで http://127.0.0.1:5000 にアクセスします。

### 静的ファイルのビルド（GitHub Pages等へのデプロイ）

freeze.py を実行して build/ ディレクトリに静的HTML/CSS/JSを出力します。

   ---


## 📝 開発メモ・注意事項

- **JavaScriptの依存関係**  
  `index.html` 内でのスクリプト読み込み順序は以下の通りです。
  1. `anomalies.js`
  2. `gameLogic.js`
  3. `main.js`

- **GitHub Pages対応**  
  静的ホスティング環境で動作させるため、異変データはAPI経由ではなく `anomalies.js` 内でJavaScriptオブジェクトとして直接管理されています。
